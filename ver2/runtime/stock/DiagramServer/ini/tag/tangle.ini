[handler]
	class="tangle" //处理器组件名称

	<input>
		class=normalize
		name=normalize
		type = 0
	</input>
	<output>
		<item>
			<params>
				//其他的输入参数，采用触发模式，及指定的参数产生或者变化时才通知处理器
				<p>
					//当指定数据不指定tag时，表示输入参数为data
					//当指定tag,不指定data时，默认为input的data
					//data_class=normalize
					//data_name=normalize
					//data_type=0
					tag_class=average
					tag_name=均线
					tag_type=5
				</p>
				<p>
					//当指定数据不指定tag时，表示输入参数为data
					//当指定tag,不指定data时，默认为input的data
					//data_class=normalize
					//data_name=normalize
					//data_type=0
					tag_class=average
					tag_name=均线
					tag_type=10
				</p>
			</params>
			<out>
				name=交叉点
				type = 0
			</out>
		</item>
		<item>
			<params>
				//其他的输入参数，采用触发模式，及指定的参数产生或者变化时才通知处理器
				<p>
					//当指定数据不指定tag时，表示输入参数为data
					//当指定tag,不指定data时，默认为input的data
					//data_class=normalize
					//data_name=normalize
					//data_type=0
					tag_class=average
					tag_name=均线
					tag_type=10
				</p>
				<p>
					//当指定数据不指定tag时，表示输入参数为data
					//当指定tag,不指定data时，默认为input的data
					//data_class=normalize
					//data_name=normalize
					//data_type=0
					tag_class=average
					tag_name=均线
					tag_type=60
				</p>
			</params>
			<out>
				name=交叉点
				type = 1
			</out>
		</item>
	</output>
